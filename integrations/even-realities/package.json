{
  "name": "carvis-even-realities",
  "version": "1.2.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "pack": "npm run build && node scripts/pack.mjs",
    "build:basic": "tsc --noEmit && vite build --mode basic",
    "pack:basic": "npm run build:basic && node scripts/pack.mjs --basic"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "0.0.15",
    "@evenrealities/pretext": "^0.1.4"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "@evenrealities/evenhub-cli": "^0.1.0"
  }
}
